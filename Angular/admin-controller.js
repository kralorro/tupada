var admin_app = angular.module("admin-app", []);
admin_app.controller("admin-controller", function($scope, $http, $interval) {
    var URL = 'http://localhost';
    var PORT = 5000;
    var API = URL + ':' + PORT;
});
