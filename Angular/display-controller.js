var login_app = angular.module("display-app", []);
login_app.controller("display-controller", function($scope, $http, $interval) {
    var URL = 'http://localhost';
    var PORT = 5000;
    var API = URL + ':' + PORT;
});
